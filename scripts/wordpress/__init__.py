"""scripts/wordpress — WordPress REST API integration.

Modules:
    wp_client     — Base REST client with Application Password auth
    wp_taxonomy   — Categories + tags discovery / create / get-or-create
    wp_media      — Media Library upload (images with alt/caption/srcset)
    wp_publisher  — End-to-end publish orchestrator (7-step flow)
"""
