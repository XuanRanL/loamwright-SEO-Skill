"""scripts/build — content assembly + format conversion.

Public modules:
    markdown_to_html        — clean WordPress-friendly HTML output
    assemble                — concatenate sections → draft.md (TODO)
    slug_generator          — SEO slug builder (TODO)
    anchor_link_builder     — ToC anchor injection (TODO)
"""
