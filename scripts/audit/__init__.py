"""Website SEO Audit scripts.

Standalone site-wide SEO auditing toolkit — independent of the blog-writer pipeline.
Shares only _core utilities (ssrf_guard, credential_hub, cost_ledger).
"""

from pathlib import Path

AUDIT_SCRIPTS_DIR = Path(__file__).parent
